import type { ReactNode } from 'react';
import { KeyboardAvoidingView, Modal, Platform, Pressable, ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import type { ThemeColors } from '@/theme/colors';
export function AccountFormModal({ visible, title, subtitle, colors, danger = false, pending, children, cancelLabel, submitLabel, onClose, onSubmit }: { visible: boolean; title: string; subtitle: string; colors: ThemeColors; danger?: boolean; pending: boolean; children: ReactNode; cancelLabel: string; submitLabel: string; onClose: () => void; onSubmit: () => void }) {
  return <Modal visible={visible} transparent animationType="fade" onRequestClose={onClose}><KeyboardAvoidingView style={styles.overlay} behavior={Platform.OS === 'ios' ? 'padding' : 'height'}><Pressable style={styles.dismiss} onPress={onClose} /><View style={[styles.card, { backgroundColor: colors.surface, borderColor: danger ? '#EF4444' : colors.border }]}><View style={styles.header}><View style={styles.copy}><Text style={[styles.title, { color: danger ? '#EF4444' : colors.text }]}>{title}</Text><Text style={[styles.subtitle, { color: colors.textSecondary }]}>{subtitle}</Text></View><TouchableOpacity onPress={onClose}><Ionicons name="close-circle" size={24} color={colors.textSecondary} /></TouchableOpacity></View><ScrollView keyboardShouldPersistTaps="handled" contentContainerStyle={styles.form}>{children}<View style={styles.buttons}><TouchableOpacity style={[styles.button, { borderColor: colors.border }]} onPress={onClose}><Text style={[styles.buttonText, { color: colors.text }]}>{cancelLabel}</Text></TouchableOpacity><TouchableOpacity disabled={pending} style={[styles.button, { backgroundColor: danger ? '#EF4444' : colors.primary, opacity: pending ? .55 : 1 }]} onPress={onSubmit}><Text style={[styles.buttonText, { color: '#FFF' }]}>{submitLabel}</Text></TouchableOpacity></View></ScrollView></View></KeyboardAvoidingView></Modal>;
}
const styles = StyleSheet.create({ overlay: { flex: 1, justifyContent: 'flex-end', backgroundColor: 'rgba(0,0,0,.62)' }, dismiss: { position: 'absolute', inset: 0 }, card: { maxHeight: '86%', borderTopLeftRadius: 26, borderTopRightRadius: 26, borderWidth: 1, padding: 20, paddingBottom: Platform.OS === 'ios' ? 34 : 22 }, header: { flexDirection: 'row', alignItems: 'flex-start', gap: 12 }, copy: { flex: 1 }, title: { fontSize: 20, fontWeight: '900' }, subtitle: { fontSize: 12, lineHeight: 18, marginTop: 4 }, form: { gap: 12, paddingTop: 18 }, buttons: { flexDirection: 'row', gap: 10, marginTop: 4 }, button: { minHeight: 46, flex: 1, borderRadius: 14, borderWidth: 1, alignItems: 'center', justifyContent: 'center' }, buttonText: { fontSize: 13, fontWeight: '900' } });
