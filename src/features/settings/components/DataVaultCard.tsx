import { StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import type { ThemeColors } from '@/theme/colors';
export function DataVaultCard({ colors, isTurkish, onBackup, onRestore, onExportCsv }: { colors: ThemeColors; isTurkish: boolean; onBackup: () => void; onRestore: () => void; onExportCsv: () => void }) {
  return <View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={styles.header}><Ionicons name="shield-checkmark" size={20} color="#10B981" /><Text style={[styles.title, { color: colors.text }]}>{isTurkish ? 'Veri kasası' : 'Data vault'}</Text></View><Text style={[styles.description, { color: colors.textSecondary }]}>{isTurkish ? 'Abonelik ve kart verilerini çevrimdışı yedekle, geri yükle veya raporla.' : 'Back up, restore, or report your subscription and card data offline.'}</Text><View style={styles.row}><VaultButton label={isTurkish ? 'JSON yedekle' : 'Back up JSON'} icon="download-outline" color="#3B82F6" onPress={onBackup} /><VaultButton label={isTurkish ? 'Geri yükle' : 'Restore'} icon="refresh-outline" color="#10B981" onPress={onRestore} /></View><VaultButton label={isTurkish ? 'CSV raporunu dışa aktar' : 'Export CSV report'} icon="document-text-outline" color="#F59E0B" onPress={onExportCsv} full /></View>;
}
function VaultButton({ label, icon, color, onPress, full = false }: { label: string; icon: 'download-outline' | 'refresh-outline' | 'document-text-outline'; color: string; onPress: () => void; full?: boolean }) { return <TouchableOpacity style={[styles.button, full ? styles.full : styles.half, { backgroundColor: `${color}1A`, borderColor: `${color}40` }]} onPress={onPress}><Ionicons name={icon} size={18} color={color} /><Text style={[styles.buttonText, { color }]}>{label}</Text></TouchableOpacity>; }
const styles = StyleSheet.create({ card: { borderRadius: 20, borderWidth: 1, padding: 16, gap: 11 }, header: { flexDirection: 'row', alignItems: 'center', gap: 8 }, title: { fontSize: 15, fontWeight: '900' }, description: { fontSize: 12, lineHeight: 18 }, row: { flexDirection: 'row', gap: 10 }, button: { minHeight: 44, borderRadius: 13, borderWidth: 1, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 6, paddingHorizontal: 10 }, half: { flex: 1 }, full: { width: '100%' }, buttonText: { fontSize: 12, fontWeight: '800', textAlign: 'center', flexShrink: 1 } });
